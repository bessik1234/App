import React from 'react';
import CardContent from '@material-ui/core/CardContent';

const Contacts = () => (
    <CardContent>
        <h1>
            Контакты
        </h1>
    </CardContent>
);

export default Contacts;